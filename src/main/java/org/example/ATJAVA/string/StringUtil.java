package org.example.ATJAVA.string;

public class StringUtil {

    public static boolean isEmpty(String str) {
        return str == null || str.isEmpty(); //return true if str is null or empty
    }
}
